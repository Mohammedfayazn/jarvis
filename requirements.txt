# in dono me se koi ek command sirf ek baar run kardo sab install ho jayga
#pip install google-genai pyaudio python-dotenv openwakeword pygetwindow pyautogui psutil pywin32
#pip install -r requirements.txt

google-genai
PyAudio
python-dotenv
# "Hey Jarvis" wake word - offline. Pehli baar chalne par ~4 MB model
# download karta hai.
openwakeword
# Window manager (window_manager.py) - Windows par apps control karna
pygetwindow
pyautogui
psutil
pywin32
# Personal memory (memory/) - semantic search ke liye optional, bhaari hai
# (~500 MB, torch samet). Ye na ho toh bhi fuzzy/keyword search chalta hai.
sentence-transformers
# Islamic tutor (islamic/) - recitation checking with a local Quran speech
# model (tarteel-ai/whisper-base-ar-quran, ~290 MB, downloaded on first use).
# Optional: lessons, audio and quizzes work without these; only
# "test my recitation" needs them. The child speech coach uses the same two
# for word practice (openai/whisper-base, ~290 MB, downloaded on first use).
torch
transformers
# WhatsApp (whatsapp_handler.py) - WhatsApp Web ko Chrome me chalata hai.
# Installed Chrome (ya Edge) hi use hota hai, alag browser download nahi.
# Ek baar link karo:  python whatsapp_handler.py login
playwright
